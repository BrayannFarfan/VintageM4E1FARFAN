<!DOCTYPE html>
<html lang="es">
   <%- include('partials/head') %>
<body>
  <%- include('partials/header') %>
    <main>
        <section>
            <h3>Basado en tu última visita</h3>
                <div class="track">
                    <% products.forEach(product => { %>
                        <%- include('partials/product', { product }) %>
                    <% })%>
                    </div>
        </section>
        <section>
            <h3 class="oferta">Ofertas</h3>
                <div>
                    <% products.forEach(product => { %>
                        <%- include('partials/product', { product }) %>
                    <% })%>
                </div>
        </section>
    </main>
   <%- include('partials/footer') %>
</body>
</html>